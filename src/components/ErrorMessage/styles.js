import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  message: {
    color: '#e63946',
    fontSize: 15,
    marginTop: 10,
    paddingHorizontal: 10,
    textAlign: 'center',
  },
});
